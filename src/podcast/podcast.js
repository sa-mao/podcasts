// eslint-disable-next-line no-empty-pattern
export default function buildMakePodCast ({ }) {
  // eslint-disable-next-line no-empty-pattern
  return function MakePodCast ({}) {
    return Object.freeze({})
  }
}
